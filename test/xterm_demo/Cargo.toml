[package]
name = "xterm_demo"
version = "0.1.0"
edition = "2024"

[dependencies]
shell-engine = { path = "../.." }
tokio = { version = "1", features = ["full"] }
axum = { version = "0.8", features = ["ws"] }
tower-http = { version = "0.6", features = ["fs"] }
futures-util = "0.3"
anyhow = "1"
